package org.usfirst.frc.team58.robot.commands;

import edu.wpi.first.wpilibj.command.CommandGroup;

public class ARightGearShoot extends CommandGroup{
	public ARightGearShoot(){
		/* T.Hansen 02.04.2017 - This is just a skeleton CommandGroup for
		 * the RightGearShoot autonomous
		 * T.Hansen 02.07.2017 - This autonomous mode, when selected, will
		 * have the robot place a gear on the rightmost lift and then
		 * shoot the 10 fuel that we start with into the high boiler.
		 * write this sort of stuff in here:
		 * addSequential(DriveForward);	
		 * addSequential(TurnLeft);
		 * addSequential(FindLift);
		 * addSequential(DriveTowardsLift);
		 * addSequential(TurnAround);
		 * addSequential(Shoot);
		 */
	
	}
}